import React from 'react'
import Layout from '../components/Layout'

type Props = {}

export default function Contact({}: Props) {
  return (
    <div>
      <Layout>
        Contacts
      </Layout>
    </div>
  )
}